spring:
  application:
    name: shop-business
  cloud:
    nacos:
      # 服务注册中心
      discovery:
        server-addr: localhost:8848
      # 配置中心
      config:
        server-addr: localhost:8848
        # 指定配置中心文件格式为yaml
        file-extension: yaml
        # 共享配置文件
        shared-dataids: mysql-redis-dev.yaml
        # refreshable-dataids=xxx.yaml